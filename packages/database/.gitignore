# Keep environment variables out of version control
.env

node_modules

# Explicitly allow tracking the build folder (overrides root .gitignore)
!build/

# Generated client - exclude the entire generated directory
prisma/generated/
