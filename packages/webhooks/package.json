{
  "name": "@undrstnd/webhooks",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@undrstnd/auth": "workspace:*",
    "svix": "^1.45.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@undrstnd/typescript-config": "workspace:*"
  }
}
